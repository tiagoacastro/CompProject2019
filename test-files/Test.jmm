class Test extends Test2 {
    int[] ola;
    int adeus;
    boolean ateamanha;

    public static void main(String[] args) {
        int i;
        int[] j;
        boolean k;
        Test2 l;

        if (i < 10 && i) {
            ola = 11[1] + 1 * 2;
            a[0] = 1;
        }
        else {
            l = j[k+1];
        }

        while (i < 10) {
            i = i + 1;
            i = i * 2 + 1;
            i = i * (2 + 1);
            a[2] * 5;
            5=5;
            k[3+3] = j/3-7;
        }
    }

    public int ola(int adeus, int[] pars) {
        boolean bool;
        int[] coiso;
        Duris d;
        int[] d2;

        bool = true;
        d = new Duris();
        d2 = new int[6];

        d = d.length;
        d = d.ola(12, 3*3, coiso);

        if (!bool) {
            bool = false;
        }
        else {
            bool = true;
        }

        pars = coiso[2+i*pars[2]];

        serere = new Teste2();
        serere = new int[2];

        return adeus;
    }
}