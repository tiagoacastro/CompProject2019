class Test extends Test2 {
    int[] ola;
    int adeus;
    boolean ateamanha;

    public static void main(String[] args) {
        int i;
        int[] j;
        boolean k;
        Test test;

        j = new int[2];

        if (i < 10 && i) {
            ola = 11[1] + 1 * 2;
            j[0] = 1;
        }
        else {
            i = j[k+1];
        }

        while (i < 10) {
            i = i + 1;
            i = i * 2 + 1;
            i = i * (2 + 1);
            a[2] * 5;
        }

        test = new Test();
        test.olaFun(1, j);
    }

    public int olaFun(int adeus, int[] pars) {
        boolean bool;
        int[] coiso;
        Duris d;
        int[] d2;

        bool = true;
        d = new Duris();
        d2 = new int[6];

        d = d.length;
        d = d.ola(12, 3*3, coiso);

        if (!bool) {
            bool = false;
        }
        else {
            bool = true;
            ola = 3;
        }

        return adeus;
    }
}